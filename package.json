{
  "name": "vonge-jekyll-bookshop-template",
  "private": true,
  "description": "Vonge template for CloudCannon",
  "scripts": {
    "jekyll:install": "bundle config set --local path 'vendor/bundle' && BUNDLE_GEMFILE=site/Gemfile bundle install",
    "build": "BUNDLE_GEMFILE=site/Gemfile bundle exec jekyll build --source site",
    "start": "BUNDLE_GEMFILE=site/Gemfile bundle exec jekyll serve --source site --port 6060 --trace --unpublished",
    "update-bookshop": "npx @bookshop/up@latest",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d _site"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yudai-yam/yudai-yam.github.io.git"
  },
  "author": "Yudai",
  "license": "MIT",
  "homepage": "https://github.com/yudai-yam/yudai-yam.github.io",
  "dependencies": {
    "@bookshop/generate": "3.16.5",
    "@bookshop/jekyll-engine": "3.16.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
